/** Общие фрагменты подсказок (одна точка правки для шапки, футера и панели результата). */
export const DATA_SOURCE_DIR = 'data_source/'
export const NPM_GENERATE_SMITHING = 'npm run generate-smithing'
/** Каталог данных → команда генерации (для футера и кратких подсказок). */
export const FORGE_DATA_SOURCE_CLI = `${DATA_SOURCE_DIR} → ${NPM_GENERATE_SMITHING}`
export const RESULT_PANEL_FILES_HINT =
  'data_source/materials, sources, materials_suffixes.txt, G_points - *.txt → npm run generate-smithing'
