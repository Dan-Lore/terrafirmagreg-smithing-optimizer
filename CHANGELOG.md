# Changelog

## 0.1.0 (2026-03-26)
- Добавлен SPA на Vue + Vite для вычисления минимальной последовательности команд, чтобы из `0` попасть в `G` (диапазон `0..150` без выхода за границы).
- Добавлен отдельный скрипт предвычисления `scripts/precompute-paths.ts`, который генерирует таблицу путей `src/generated/paths.ts`.
- Добавлена базовая спецификация (`SPECIFICATION.md`) и конфигурация проекта (`.gitignore`).

